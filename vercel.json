{
  "buildCommand": "cd artifacts/hamza-portfolio && pnpm run build",
  "outputDirectory": "artifacts/hamza-portfolio/dist",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile"
}
